# CMake build directories (rri_cpu, rri_cl, tests, CTest state)
build/
build-*/
cmake-build-*/

# Compiled objects and binaries
*.o
*.obj
*.so
*.a
*.exe

# Editor/OS cruft
*.swp
*~
.DS_Store

# solo30s comparison-run outputs (regenerated by running the model, not
# source -- keep the input dataset itself if it's ever populated under
# solo30s_input/, but not run artifacts like these)
hydro.txt
hydro_hr.txt
storage.dat
*.out
